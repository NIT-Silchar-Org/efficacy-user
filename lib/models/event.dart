class Event {
  String id, name, club, date, time, description, url;
  Event(
      {this.id,
      this.name,
      this.club,
      this.date,
      this.time,
      this.description,
      this.url});
}
